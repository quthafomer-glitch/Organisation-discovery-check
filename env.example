# These are not read from a .env file in production — Vercel injects
# environment variables set in the dashboard (or via `vercel env add`).
# This file just documents what's needed, and is used by `vercel dev` for
# local testing if you create a real .env from it.

OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.1
